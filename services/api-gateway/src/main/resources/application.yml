server:
  port: 8080

spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: user-service
              uri: http://localhost:8082
              predicates:
                - Path=/users/**
              # ✅ NO filters here (GlobalFilter is used)

# 🔐 JWT config (THIS WAS MISSING – ROOT CAUSE)
jwt:
  secret: flowforge-super-secure-jwt-secret-256-bit-minimum-key!!


management:
  endpoints:
    web:
      exposure:
        include: gateway,health,info
